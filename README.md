# Canadian Car and Truck Rentals, Sales & Leasing

Generated by the SiteGenerator cinematic-site-generator skill on 2026-04-28.
Slug: `canadian-car-and-truck-rentals-sales-lea-c70bf1`.

## Generation fingerprint

- **Archetype**: G — Mixed-Media Hybrid
- **Style**: S5 — Industrial Precision
- **Voice**: V6 (industrial / technical), locked by positioning brief
- **Hero treatment**: scrubbed-frames (Veo 3.1 Lite + ffmpeg, 192 frames @ 24fps)
- **Header**: split-utility
- **Footer**: FT1 Classic 5-Col
- **Color**: graphite-amber (dark luminance, amber `#ffaa40` accent)
- **Motion bg**: beam-rays @ subtle density (1 section per page)
- **CTA section**: CTA1 centered oversized type
- **About / Benefits / Contact**: AB2 / BN1 / CT1
- **Stats**: ST1 (3 counters: 19 classes / 187+ reviews / 3 service lines)

## Scope reductions in this run

- **Step 4.5 — Supabase provisioning** is **DEFERRED**: the org's
  free-tier project limit (2 projects per admin) was already reached.
  Existing projects (`kodagen-master`, `kodagen-springhill-hotel`) were
  not paused/deleted without explicit authorization. The full schema +
  seed SQL is committed at `provision/admin-schema.sql` +
  `provision/seed-data.sql`; the runbook is at
  `provision/PROVISION-LATER.md`. Running `node _gen/provision-supabase.mjs`
  once a slot opens completes Step 4.5 idempotently.
- **Engine-specific schemas** (booking / catalog / crm / tickets) from
  `templates/tenant-admin/supabase/migrations/` are not present in the
  repo (only baseline migrations 0005–0007 exist). The shipped schema is
  a lean engine-agnostic admin baseline (sites, admin_users, inquiries,
  reservations, inventory, audit_log) — sufficient for this auto-rental
  business but not the full multi-engine matrix the SKILL.md describes.

The public site ships fully functional. Routes that read from Supabase
(`/admin/*`, `/api/contact`, `/api/reserve`) detect the placeholder env
vars at runtime and degrade gracefully:
- `/admin/*` redirects to `/admin/login` with a "pending" notice
- Form submissions log to console + return success
- Inventory + copy render from `content/refined-copy.json` (static)

When provisioning runs later, no code changes are needed.

## Asset generation summary (Step 4)

| Asset | Source | Output |
|---|---|---|
| Hero scene-1-start.jpg | Nano Banana 3.1 Flash | `public/frames-source/scene-1-start.jpg` (789 KB) |
| Hero scene-1-end.jpg | Nano Banana 3.1 Flash | `public/frames-source/scene-1-end.jpg` (660 KB) |
| Section: fleet-grid | Nano Banana 3.1 Flash | `public/section-fleet-grid.jpg` (992 KB) |
| Section: truck-detail | Nano Banana 3.1 Flash | `public/section-truck-detail.jpg` (685 KB) |
| Section: showroom | Nano Banana 3.1 Flash | `public/section-showroom.jpg` (672 KB) |
| Section: cube-van | Nano Banana 3.1 Flash | `public/section-cube-van.jpg` (862 KB) |
| Hero video clip | Veo 3.1 Lite (720p, 8s, with start+end keyframes) | `raw/scene-1.mp4` (4.56 MB) |
| Scroll frames | ffmpeg @ 24fps × 1920px | `public/frames/frame_*.jpg` (192 frames, 23 MB) |

## Run locally

```bash
npm install
npm run dev
# open http://localhost:3000
```

## Build for production

```bash
npm run typecheck
npm run build
npm start
```

## Routes

| Path | Description |
|---|---|
| `/` | Home — scroll-scrubbed cinematic hero, services, fleet grid, benefits, motion-bg breather, inventory teaser, CTA |
| `/services` | Services index |
| `/services/[slug]` | Per-service detail (6 services) |
| `/about` | About page (industry hero + values) |
| `/contact` | Contact page (form, hours, address, embedded map) |
| `/reserve` | 3-step reservation wizard |
| `/admin/login` | Admin sign-in (post-provisioning) |
| `/admin` | Admin dashboard (post-provisioning) |
| `/sitemap.xml` | Build-time sitemap |
| `/robots.txt` | Build-time robots |

## Production hardening rules verified

- Rule 1: `overflow-x-clip` everywhere (no `overflow-x-hidden`)
- Rule 4 / 5: no HEAD or Range probes
- Rule 20: `middleware.ts` gates `/admin/*`
- Rule 21: `SUPABASE_SERVICE_ROLE_KEY` only read in server contexts; never inlined into client components
- Rule 23: layout uses `overflow-x-clip`
- Rule 24: every public page declares `revalidate` or `dynamic`
- Rule 26: middleware refreshes Supabase session
- Rule 27: no `NEXT_PUBLIC_*` secrets
- Rule 30: every CTA traces to `positioning-brief.json`'s `primary_cta_options` / `secondary_cta_options`
- Rule 32: SEO baseline — per-page metadata, JSON-LD (Organization, WebSite, AutoRental+AutoDealer LocalBusiness, per-service Service + Breadcrumb), sitemap, robots
- Rule 33 considered: this brand voice is V6 (industrial), not V1; rule 33 premium gate does not apply but the site still meets the premium quality bar via 3-font system + spacing scale + brushed-card material + scroll-scrubbed video hero + amber accent only
